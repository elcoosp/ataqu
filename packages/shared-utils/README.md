# @ataqu/shared-utils

Pure utility functions.

- `generateIdempotencyKey` – UUID v4.
- `formatDate`, `formatCurrency` – using Intl.
- `truncateText`, `buildQueryString`.
- `handleApiError` – transforms errors.
- `deepClone`, `sleep`.
