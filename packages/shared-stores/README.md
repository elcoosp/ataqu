# @ataqu/shared-stores

Zustand stores.

- `useAuthStore` – authentication state (persisted).
- `useUIStore` – sidebar, theme, focus mode (persisted).
- `useOnboardingStore` – completed tours (persisted).
