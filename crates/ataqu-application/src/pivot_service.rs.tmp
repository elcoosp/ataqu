use ataqu_domain_pivot::{
    document::{CreateDocumentCommand, Document, DocumentCreatedEvent, create_document},
    block::{CreateRelationCommand, Relation, create_relation},
    primitives::{Clock, IdGenerator, TenantId, Uuid as DomainUuid},
};
use chrono::{DateTime, Utc};
use sea_orm::DatabaseConnection;
use sqlx::{PgPool, Row};
use std::sync::Arc;
use thiserror::Error;
use tracing::{info, instrument};
use uuid::Uuid as StdUuid;
