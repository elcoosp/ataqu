[package]
name = "ataqu-security"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.8.5"

aes-gcm = { version = "0.10", features = ["std"] }
base64 = "0.22"
serde = { version = "1.0", features = ["derive"] }
[features]
infra-pii-access = []
